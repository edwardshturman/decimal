"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
archive = "rm -rf node_modules/"
run = "bun dev"
setup = "vc link --scope compsigh --project decimal --yes && vc env pull && bun install"
